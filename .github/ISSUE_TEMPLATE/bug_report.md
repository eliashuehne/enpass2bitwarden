---
name: Bug report
about: Something broke during migration
labels: bug
---

**Tool version** (output of `enpass2bitwarden --version`):

**OS / platform:**

**Input type:** `.enpassbackup` file / raw `vault.enpassdb` + sidecar files

**Command you ran:**

```
(paste command — remove the master password if it was inline!)
```

**Output** (full, including `warn:` lines):

```
(paste output — redact item titles/usernames if needed)
```

**What happened vs what you expected:**

